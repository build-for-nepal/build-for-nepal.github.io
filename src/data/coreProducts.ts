import { PROJECTS } from "./projects";

// First entry is the featured hero card.
// To show a subset, filter PROJECTS by id here.
export const CORE_PRODUCTS = PROJECTS;

export const FEATURED_PRODUCT_ID = PROJECTS[0].id;
